# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = compose:caps
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# Gestures configuration was removed in Hyprland v0.50.
# The former `gestures { workspace_swipe = … }` group no longer exists
# and will cause a config error if left enabled. If you need swipe
# gestures, refer to the Hyprland documentation for plugin-based or
# external gesture solutions compatible with your version.
#
# Example (pre-0.50, now deprecated):
# gestures {
#     workspace_swipe = false
# }
